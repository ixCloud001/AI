name: remotedesktopmacos
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  network_info_plus: ^7.0.0
  flutter_webrtc: ^0.11.7
  socket_io_client: ^2.0.3+1
  pasteboard: ^0.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
